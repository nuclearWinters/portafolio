import React from 'react';

const Error = props => (
  <div style={{height: '100%'}}>
    <div className="main">  
      <div className="description-cenz">
          Lo sentimo mucho pero la pagina que usted quiere ver no existe en este sitio ¡Pruebe con otra!
      </div>
    </div>
  </div>
);

export default Error;